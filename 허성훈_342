class Solution {
public:
    bool isPowerOfFour(int num) {
        while(num >= 4) {
            if(num % 4 != 0) {
                return false;
            }
            num /= 4;
        }
        return num == 1 ? true : false;
    }
};
